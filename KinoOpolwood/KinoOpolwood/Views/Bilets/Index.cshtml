@model IEnumerable<KinoOpolwood.Models.Bilet>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Miejsce.MiejsceId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Seans.SeansId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Miejsce.MiejsceId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Seans.SeansId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BiletId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BiletId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BiletId })
        </td>
    </tr>
}

</table>
